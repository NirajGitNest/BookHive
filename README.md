# BookHive

A project that includes:
- **BookHive/** — the main application
- **Database/** — SQL scripts to create and populate the database

## How to run
1. Import the `.sql` file from `Database/` into your database.
2. Open `BookHive/` in your IDE or run the application as per your setup.
